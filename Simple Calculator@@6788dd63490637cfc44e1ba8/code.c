#include <stdio.h>

int main() {
    int a,b,operation;
    scanf("%d %d",&a,&b);
    if(operator==+)
    printf(a+b);
    else if(operator==-)
    printf(a-b);
    else if(operator==*)
    printf(a*b);
    else if(operator==/)
    printf(a/b);

    return 0;
}