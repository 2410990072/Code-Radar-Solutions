#include <stdio.h>
#include <ctype.h>
#include <string.h>

void main() {
    char x;
    scanf("%c",&x);
    printf("%s",isdigit(a) ? "Digit":(isaplpha(a) ? (strchr("aeiuoAEIOU",a) ? "Vowel": "Consonant"):"Special Character"));
    
    

}